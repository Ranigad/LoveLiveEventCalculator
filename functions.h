#ifndef FUNCTIONS_H
#define FUNCTIONS_H

	void stringFormatter(std::string&);
	int rankToInt(char rank);

	void epCalculator(User);
	void updateData(User);
	int pointsPerGame(std::string, char, char, bool);

	int maxExp(int);
	void userInfo(User);
	
#endif